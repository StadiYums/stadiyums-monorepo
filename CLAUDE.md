@AGENTS.md

# Claude session notes — StadiYums

After implementation (required):

1. Run **atomic Karma commits** — smallest sensible Conventional Commits (`type(scope): summary`), one logical change each.
2. **Push** the branch to `origin` (`git push -u origin HEAD` on first push).
3. Do not leave finished work sitting uncommitted or unpushed.
4. Open/update the PR with a descriptive title and body.
